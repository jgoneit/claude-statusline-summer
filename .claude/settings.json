{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in */statusline.sh) bash -n \"$f\" || { echo \"statusline.sh failed bash -n syntax check.\" >&2; exit 2; } ;; esac",
            "statusMessage": "Checking statusline.sh syntax"
          }
        ]
      }
    ]
  }
}
