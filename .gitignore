# Machine-specific Claude Code settings (contains an absolute path — not portable)
.claude/settings.local.json

# Claude Code — local agent / worktree artifacts (generated, per-machine)
# Shared .claude/settings.json and any .claude/{agents,commands,skills} stay committed.
.claude/worktrees/
CLAUDE.local.md

# macOS
.DS_Store

# Status line per-session git cache (normally lives in /tmp; ignore if it lands here)
statusline-summer-*
